name: meme_generator
description: Meme generator made with Flutter

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2
  image_picker: ^0.6.1
  path_provider: ^1.4.0
  firebase_core: ^0.4.0+9
  firebase_analytics: ^5.0.2
  firebase_admob: 0.9.0+7
#  admob_flutter: ^0.3.1
  cloud_firestore: ^0.12.9
  http: ^0.12.0
  flutter_cache_manager: ^1.1.3
  cached_network_image: ^1.1.2
  esys_flutter_share: ^1.0.2
  permission_handler: '^3.3.0'

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:

  uses-material-design: true

